import 'package:flame/game.dart';

/// Root game class. Manages the game loop and scene routing.
class NudGame extends FlameGame {
  @override
  Future<void> onLoad() async {
    await super.onLoad();
    // TODO: load initial scene (e.g. MainMenuScene)
  }
}
